# Packaging Assets

`joyboy.ico` is generated from `web/static/images/monogramme.png` and is used
for Windows desktop builds.

Regenerate it with:

```powershell
venv\Scripts\python.exe scripts\generate_app_icons.py
```
